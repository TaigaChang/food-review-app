// Global constants for the client application

// Auth polling interval (milliseconds) - check every 30 seconds for session expiry
export const POLL_INTERVAL_MS = 30 * 1000; // 30 seconds

// Add other client-side constants here as needed
// export const SOME_OTHER_CONSTANT = 100;
